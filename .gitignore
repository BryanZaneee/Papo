# Audio files (too large for git)
*.wav
*.WAV

# OS files
.DS_Store
Thumbs.db

# Screenshot folders (reference only)
tree-view/
footer-view/
mobile-view/

# Local tooling — Playwright caches
.playwright-cli/
.playwright-mcp/

# Agent / Claude workflow artifacts (local notes, not site content)
AGENTS.md
History.md
issues/
plans/

# Design source files (large binaries, not for web)
*.psd
*.PSD
